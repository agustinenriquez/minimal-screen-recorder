# Screen Recorder Dependencies

# Core recording libraries
opencv-python>=4.8.0
mss>=9.0.0
numpy>=1.24.0

# Audio processing
# Note: PulseAudio (pactl) and FFmpeg are system dependencies
# Install with: sudo apt install pulseaudio-utils ffmpeg

# Optional dependencies for enhanced features
Pillow>=10.0.0  # For additional image processing
psutil>=5.9.0   # For system monitoring and process management
